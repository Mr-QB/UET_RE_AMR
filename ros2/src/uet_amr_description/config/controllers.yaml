controller_manager:
  ros__parameters:
    update_rate: 100 # [Hz]
    use_sim_time: false

    diff_drive_controller:
      type: diff_drive_controller/DiffDriveController

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

diff_drive_controller:
  ros__parameters:
    left_wheel_names: ["left_wheel_joint"]
    right_wheel_names: ["right_wheel_joint"]

    wheel_separation: 0.45  # [m] -- measured on the real robot
    wheel_radius: 0.08255   # [m] -- matches uet_amr.xacro's wheel_radius property

    use_stamped_vel: false

    wheel_separation_multiplier: 1.0
    left_wheel_radius_multiplier: 1.0
    right_wheel_radius_multiplier: 1.0

    publish_rate: 50.0 # [Hz]
    odom_frame_id: "odom"
    base_frame_id: "base_footprint"
    pose_covariance_diagonal: [0.001, 0.001, 0.0, 0.0, 0.0, 0.01]
    twist_covariance_diagonal: [0.001, 0.0, 0.0, 0.0, 0.0, 0.01]

    open_loop: false
    enable_odom_tf: false
    cmd_vel_timeout: 0.5 # [s]

    # Velocity and acceleration limits (real-hardware's more conservative
    # cap; raise linear.x.max_velocity for sim if faster testing is wanted)
    linear.x.has_velocity_limits: true
    linear.x.has_acceleration_limits: true
    linear.x.max_velocity: 0.4         # [m/s]
    linear.x.max_acceleration: 0.2     # [m/s^2]

    angular.z.has_velocity_limits: true
    angular.z.has_acceleration_limits: true
    angular.z.max_velocity: 2.0        # [rad/s]
    angular.z.max_acceleration: 4.0    # [rad/s^2]
